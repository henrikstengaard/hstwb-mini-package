; amiga boot selector hstgemos1mb
IF EXISTS S:HSTLauncherMenu
  execute S:HSTLauncherMenu "HSTGames1MB"
ELSE
  REQUESTCHOICE "Error" "HST Launcher Menu is not installed!" "OK" >NIL:
ENDIF
wait 1
totalreset